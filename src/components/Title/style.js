import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  textStyle: {
    fontSize: 30,
    fontWeight: 'bold',
  },
});

export default styles;
